This directory is reserved by the Voice Platform model distribution tree (catalog v0.1).
No runtime model artifacts are committed to the repository (ADR-0015). Real artifact files are published here by the distribution workflow, not committed.
See ..\..\README.md (tree root) for conventions.
